# vLLM Manager 環境変数設定
# このファイルを .env にコピーして編集してください（docker compose が読み込みます）

# === プロキシ（社内ネット・研究機関などで必須になる場合）===
# compose の build と各サービス（backend / frontend ビルド / litellm / 任意の vLLM など）に渡されます。
# npm / pip / apt / Hugging Face 経由のダウンロードが失敗する場合は設定を検討してください。
#
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# カンマ区切り。ローカルホスト・コンテナ間通信をプロキシに送らないために推奨です。
# NO_PROXY=localhost,127.0.0.1,::1,backend,litellm,litellm-db,frontend,.local

# === vLLM サーバー設定 ===
VLLM_PORT=8001
VLLM_MAX_MODEL_LEN=8192
VLLM_MAX_NUM_SEQS=256
VLLM_GPU_MEMORY_UTILIZATION=0.9
VLLM_TENSOR_PARALLEL_SIZE=1

# === LiteLLM 認証 ===
# 空にすると認証なし（公開モード）
# 値を設定すると Bearer Token 認証が有効になる
LITELLM_MASTER_KEY=sk-vllm-default-key
LITELLM_UI_USERNAME=admin
LITELLM_UI_PASSWORD=admin
LITELLM_DB_USER=litellm
LITELLM_DB_PASSWORD=litellm
LITELLM_DB_NAME=litellm

# === vLLM Manager 認証 ===
VLLM_MANAGER_ADMIN_USER=admin
VLLM_MANAGER_ADMIN_PASSWORD=admin

# === HuggingFace ===
# モデルダウンロードに必要（大規模モデルの場合）
# HF_TOKEN=your-hf-token

# === アプリケーション ===
BACKEND_PORT=8000
FRONTEND_PORT=3000
LITELLM_PORT=4000

# フロントエンドから見える API の公開 URL（必要なら上書き）
# 空にすると frontend(:13000) の同一オリジン経由で backend にプロキシします（推奨）
# 例: NEXT_PUBLIC_API_URL=http://hinton.prv.kanazawa-it.ac.jp:18000
# 例: NEXT_PUBLIC_LITELLM_URL=http://hinton.prv.kanazawa-it.ac.jp:14000
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_LITELLM_URL=http://hinton.prv.kanazawa-it.ac.jp:14000
BACKEND_INTERNAL_URL=http://backend:8000

# === Docker ===
DOCKER_COMPOSE_PROJECT_NAME=vllm-manager
